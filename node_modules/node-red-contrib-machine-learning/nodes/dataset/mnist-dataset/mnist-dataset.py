import json
import pandas
import os
import numpy as np

config = json.loads(input())

while True:
	#input()
        path = config['path']
        f = np.load
        x_train,y_train = f['x_train'],f['y_train']
        f.close()

	x_train = np.reshape(x_train,(60000,784))
        y_train = np.reshape(y_train,(60000,1))

	x_train = np.concatenate((x_train,y_train),axis=1)

        df_train = pandas.DataFrame(x_train)
        r,c = df_train.shape

        first_column = 0
        last_coulumn = c
        df = df_train.iloc[:,first_column:last_coulumn]
        res = df.to_json(orient = 'values') 
        print(res)
